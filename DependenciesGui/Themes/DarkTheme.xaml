<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Palette -->
    <Color x:Key="Dark.Background0">#FF1E1E1E</Color>
    <Color x:Key="Dark.Background1">#FF252526</Color>
    <Color x:Key="Dark.Background2">#FF2D2D30</Color>
    <Color x:Key="Dark.Background3">#FF3E3E42</Color>
    <Color x:Key="Dark.Border">#FF3F3F46</Color>
    <Color x:Key="Dark.BorderLight">#FF555555</Color>
    <Color x:Key="Dark.Text">#FFF1F1F1</Color>
    <Color x:Key="Dark.GrayText">#FF8A8A8A</Color>
    <Color x:Key="Dark.Accent">#FF007ACC</Color>
    <Color x:Key="Dark.Selection">#FF264F78</Color>

    <SolidColorBrush x:Key="Dark.Background0Brush" Color="{StaticResource Dark.Background0}" />
    <SolidColorBrush x:Key="Dark.Background1Brush" Color="{StaticResource Dark.Background1}" />
    <SolidColorBrush x:Key="Dark.Background2Brush" Color="{StaticResource Dark.Background2}" />
    <SolidColorBrush x:Key="Dark.Background3Brush" Color="{StaticResource Dark.Background3}" />
    <SolidColorBrush x:Key="Dark.BorderBrush" Color="{StaticResource Dark.Border}" />
    <SolidColorBrush x:Key="Dark.BorderLightBrush" Color="{StaticResource Dark.BorderLight}" />
    <SolidColorBrush x:Key="Dark.TextBrush" Color="{StaticResource Dark.Text}" />
    <SolidColorBrush x:Key="Dark.GrayTextBrush" Color="{StaticResource Dark.GrayText}" />
    <SolidColorBrush x:Key="Dark.AccentBrush" Color="{StaticResource Dark.Accent}" />
    <SolidColorBrush x:Key="Dark.SelectionBrush" Color="{StaticResource Dark.Selection}" />

    <!-- Application specific brushes (dark) -->
    <SolidColorBrush x:Key="Theme.AppBackground" Color="{StaticResource Dark.Background1}" />
    <SolidColorBrush x:Key="Theme.ListBackground" Color="{StaticResource Dark.Background0}" />
    <SolidColorBrush x:Key="Theme.ListForeground" Color="{StaticResource Dark.Text}" />
    <SolidColorBrush x:Key="Theme.ListSelectionBackground" Color="{StaticResource Dark.Selection}" />
    <SolidColorBrush x:Key="Theme.ListSelectionForeground" Color="White" />
    <SolidColorBrush x:Key="Theme.ListHoverBackground" Color="{StaticResource Dark.Background3}" />
    <SolidColorBrush x:Key="Theme.ListBorder" Color="{StaticResource Dark.Border}" />
    <SolidColorBrush x:Key="Theme.GroupHeaderBackground" Color="{StaticResource Dark.Background3}" />
    <SolidColorBrush x:Key="Theme.GroupHeaderBorder" Color="{StaticResource Dark.BorderLight}" />
    <SolidColorBrush x:Key="Theme.SearchFolderBackground" Color="{StaticResource Dark.Background2}" />
    <SolidColorBrush x:Key="Theme.FilterBorder" Color="{StaticResource Dark.Border}" />
    <SolidColorBrush x:Key="Theme.FilterHint" Color="{StaticResource Dark.GrayText}" />
    <SolidColorBrush x:Key="Theme.FilterInputBorder" Color="{StaticResource Dark.Border}" />
    <SolidColorBrush x:Key="Theme.ClearButtonHoverBackground" Color="{StaticResource Dark.Background3}" />
    <SolidColorBrush x:Key="Theme.DisabledMenuItemBackground" Color="Transparent" />

    <!-- Dragablz tab overrides -->
    <SolidColorBrush x:Key="TabItem.Static.Background" Color="{StaticResource Dark.Background2}" />
    <SolidColorBrush x:Key="TabItem.Static.Border" Color="{StaticResource Dark.Border}" />
    <SolidColorBrush x:Key="TabItem.Selected.Background" Color="{StaticResource Dark.Background0}" />
    <SolidColorBrush x:Key="TabItem.Selected.Border" Color="{StaticResource Dark.BorderLight}" />
    <SolidColorBrush x:Key="TabItem.Foreground" Color="{StaticResource Dark.Text}" />

    <!-- System colors overrides -->
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="{StaticResource Dark.Background0}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="{StaticResource Dark.Text}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="{StaticResource Dark.Background2}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="{StaticResource Dark.Text}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlLightBrushKey}" Color="{StaticResource Dark.Background3}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlLightLightBrushKey}" Color="{StaticResource Dark.Background3}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlDarkBrushKey}" Color="{StaticResource Dark.Border}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlDarkDarkBrushKey}" Color="{StaticResource Dark.BorderLight}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="{StaticResource Dark.Background2}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBarBrushKey}" Color="{StaticResource Dark.Background2}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="{StaticResource Dark.Text}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuHighlightBrushKey}" Color="{StaticResource Dark.Background3}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="{StaticResource Dark.Selection}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White" />
    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="{StaticResource Dark.Background3}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="{StaticResource Dark.Text}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.GrayTextBrushKey}" Color="{StaticResource Dark.GrayText}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.InfoBrushKey}" Color="{StaticResource Dark.Background2}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.InfoTextBrushKey}" Color="{StaticResource Dark.Text}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.ActiveBorderBrushKey}" Color="{StaticResource Dark.Border}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveBorderBrushKey}" Color="{StaticResource Dark.Border}" />

    <!-- Labels / text -->
    <Style TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
    </Style>

    <Style TargetType="{x:Type ToolTip}">
        <Setter Property="Background" Value="{StaticResource Dark.Background2Brush}" />
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Dark.BorderLightBrush}" />
    </Style>

    <!-- Button -->
    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource Dark.Background3Brush}" />
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Dark.BorderLightBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          Focusable="False" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDefaulted" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Dark.AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Dark.BorderLightBrush}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Dark.AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Dark.AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Dark.Background2Brush}" />
                            <Setter Property="Foreground" Value="{StaticResource Dark.GrayTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox -->
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{StaticResource Dark.Background0Brush}" />
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Dark.BorderLightBrush}" />
        <Setter Property="CaretBrush" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="SelectionBrush" Value="{StaticResource Dark.AccentBrush}" />
    </Style>

    <!-- ListBox / ListView / TreeView -->
    <Style TargetType="{x:Type ListBox}">
        <Setter Property="Background" Value="{StaticResource Dark.Background0Brush}" />
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Dark.BorderBrush}" />
    </Style>

    <Style TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="{StaticResource Dark.Background0Brush}" />
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Dark.BorderBrush}" />
    </Style>

    <Style TargetType="{x:Type TreeView}">
        <Setter Property="Background" Value="{StaticResource Dark.Background0Brush}" />
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Dark.BorderBrush}" />
    </Style>

    <!-- GridView column header (the styles themselves live in DependencyCustomListView.xaml
         so that they follow theme changes at runtime) -->
    <SolidColorBrush x:Key="Theme.HeaderBackground" Color="{StaticResource Dark.Background2}" />
    <SolidColorBrush x:Key="Theme.HeaderForeground" Color="{StaticResource Dark.Text}" />
    <SolidColorBrush x:Key="Theme.HeaderBorder" Color="{StaticResource Dark.Border}" />
    <SolidColorBrush x:Key="Theme.HeaderHoverBackground" Color="{StaticResource Dark.Background3}" />
    <SolidColorBrush x:Key="Theme.HeaderPressedBackground" Color="{StaticResource Dark.Selection}" />

    <!-- ComboBox -->
    <ControlTemplate x:Key="Dark.ComboBoxToggleButtonTemplate" TargetType="{x:Type ToggleButton}">
        <Border x:Name="border"
                Background="{StaticResource Dark.Background2Brush}"
                BorderBrush="{StaticResource Dark.BorderLightBrush}"
                BorderThickness="1"
                SnapsToDevicePixels="True">
            <Path x:Name="arrow"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Center"
                  Margin="0,0,6,0"
                  Data="M0,0 L4,4 L8,0 Z"
                  Fill="{StaticResource Dark.TextBrush}" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Background" Value="{StaticResource Dark.Background3Brush}" />
                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Dark.AccentBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Dark.AccentBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="arrow" Property="Fill" Value="{StaticResource Dark.GrayTextBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="Background" Value="{StaticResource Dark.Background2Brush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Dark.BorderLightBrush}" />
        <Setter Property="Padding" Value="6,3,20,3" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <ToggleButton Template="{StaticResource Dark.ComboBoxToggleButtonTemplate}"
                                      Focusable="False"
                                      ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          IsHitTestVisible="False"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" />
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide"
                               IsOpen="{TemplateBinding IsDropDownOpen}">
                            <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="{StaticResource Dark.Background2Brush}"
                                    BorderBrush="{StaticResource Dark.BorderLightBrush}"
                                    BorderThickness="1">
                                <ScrollViewer SnapsToDevicePixels="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Dark.GrayTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="Padding" Value="6,3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="border" Background="Transparent" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Dark.Background3Brush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Dark.SelectionBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menus -->
    <Style TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="{StaticResource Dark.Background2Brush}" />
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
    </Style>

    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="{x:Type Separator}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border Height="1" Margin="4,0" VerticalAlignment="Center" Background="{StaticResource Dark.BorderLightBrush}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ContextMenu}">
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Border Background="{StaticResource Dark.Background2Brush}"
                            BorderBrush="{StaticResource Dark.BorderLightBrush}"
                            BorderThickness="1"
                            Padding="2">
                        <ScrollViewer CanContentScroll="True"
                                      Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type FrameworkElement}, ResourceId=MenuScrollViewer}}">
                            <ItemsPresenter Grid.IsSharedSizeScope="True"
                                            KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type MenuItem}">
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="6,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="IconColumn" Width="Auto" SharedSizeGroup="MenuItemIconColumnGroup" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition x:Name="GestureColumn" Width="Auto" SharedSizeGroup="MenuItemGestureColumnGroup" />
                                <ColumnDefinition x:Name="ArrowColumn" Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Grid x:Name="IconArea" Grid.Column="0" Width="22" Margin="2,0,4,0">
                                <ContentPresenter x:Name="Icon"
                                                  ContentSource="Icon"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center" />
                                <Path x:Name="CheckMark"
                                      Visibility="Collapsed"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      Data="M0,4 L3,7 L9,0"
                                      Stroke="{TemplateBinding Foreground}"
                                      StrokeThickness="1.5" />
                            </Grid>

                            <ContentPresenter x:Name="HeaderHost"
                                              Grid.Column="1"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True" />

                            <TextBlock x:Name="InputGestureText"
                                       Grid.Column="2"
                                       Margin="16,0,6,0"
                                       VerticalAlignment="Center"
                                       Text="{TemplateBinding InputGestureText}" />

                            <Path x:Name="Arrow"
                                  Grid.Column="3"
                                  Margin="4,0,6,0"
                                  VerticalAlignment="Center"
                                  Data="M0,0 L4,4 L0,8 Z"
                                  Fill="{TemplateBinding Foreground}" />

                            <Popup x:Name="PART_Popup"
                                   Placement="Right"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border Background="{StaticResource Dark.Background2Brush}"
                                        BorderBrush="{StaticResource Dark.BorderLightBrush}"
                                        BorderThickness="1"
                                        Padding="2">
                                    <ScrollViewer CanContentScroll="True"
                                                  Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type FrameworkElement}, ResourceId=MenuScrollViewer}}">
                                        <ItemsPresenter Grid.IsSharedSizeScope="True"
                                                        KeyboardNavigation.DirectionalNavigation="Cycle"
                                                        KeyboardNavigation.TabNavigation="Cycle" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="IconArea" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IconColumn" Property="SharedSizeGroup" Value="{x:Null}" />
                            <Setter TargetName="GestureColumn" Property="SharedSizeGroup" Value="{x:Null}" />
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom" />
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter TargetName="IconArea" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IconColumn" Property="SharedSizeGroup" Value="{x:Null}" />
                            <Setter TargetName="GestureColumn" Property="SharedSizeGroup" Value="{x:Null}" />
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Hidden" />
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Dark.Background3Brush}" />
                        </Trigger>
                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Dark.Background3Brush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Dark.GrayTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- StatusBar -->
    <Style TargetType="{x:Type StatusBar}">
        <Setter Property="Background" Value="{StaticResource Dark.Background2Brush}" />
        <Setter Property="Foreground" Value="{StaticResource Dark.TextBrush}" />
    </Style>

    <!-- GridSplitter -->
    <Style TargetType="{x:Type GridSplitter}">
        <Setter Property="Background" Value="{StaticResource Dark.Background1Brush}" />
    </Style>

    <!-- ScrollBar -->
    <Style x:Key="Dark.ScrollBarLineButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border x:Name="border" Background="Transparent">
                        <Path x:Name="glyph"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Data="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}"
                              Fill="{StaticResource Dark.GrayTextBrush}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="glyph" Property="Fill" Value="{StaticResource Dark.TextBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="glyph" Property="Fill" Value="{StaticResource Dark.AccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Dark.ScrollBarPageButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Dark.ScrollBarThumb" TargetType="{x:Type Thumb}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border x:Name="thumb" Margin="3" Background="{StaticResource Dark.BorderLightBrush}" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="thumb" Property="Background" Value="{StaticResource Dark.GrayTextBrush}" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="thumb" Property="Background" Value="{StaticResource Dark.TextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="{StaticResource Dark.Background1Brush}" />
        <Setter Property="Width" Value="16" />
        <Setter Property="MinWidth" Value="16" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="16" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="16" />
                        </Grid.RowDefinitions>
                        <RepeatButton Grid.Row="0" Style="{StaticResource Dark.ScrollBarLineButton}" Command="ScrollBar.LineUpCommand" Content="M0,4 L4,0 L8,4 Z" />
                        <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource Dark.ScrollBarPageButton}" Command="ScrollBar.PageUpCommand" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource Dark.ScrollBarThumb}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource Dark.ScrollBarPageButton}" Command="ScrollBar.PageDownCommand" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                        <RepeatButton Grid.Row="2" Style="{StaticResource Dark.ScrollBarLineButton}" Command="ScrollBar.LineDownCommand" Content="M0,0 L8,0 L4,4 Z" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="Height" Value="16" />
                <Setter Property="MinHeight" Value="16" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="16" />
                                </Grid.ColumnDefinitions>
                                <RepeatButton Grid.Column="0" Style="{StaticResource Dark.ScrollBarLineButton}" Command="ScrollBar.LineLeftCommand" Content="M4,0 L0,4 L4,8 Z" />
                                <Track x:Name="PART_Track" Grid.Column="1">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Style="{StaticResource Dark.ScrollBarPageButton}" Command="ScrollBar.PageLeftCommand" />
                                    </Track.DecreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource Dark.ScrollBarThumb}" />
                                    </Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Style="{StaticResource Dark.ScrollBarPageButton}" Command="ScrollBar.PageRightCommand" />
                                    </Track.IncreaseRepeatButton>
                                </Track>
                                <RepeatButton Grid.Column="2" Style="{StaticResource Dark.ScrollBarLineButton}" Command="ScrollBar.LineRightCommand" Content="M0,0 L4,4 L0,8 Z" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
